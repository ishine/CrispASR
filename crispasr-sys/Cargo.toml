[package]
name = "crispasr-sys"
version = "0.1.1"
edition = "2021"
description = "Raw FFI bindings to CrispASR (whisper.cpp fork) C API"
license = "MIT"
links = "whisper"

[build-dependencies]
cmake = "0.1"

[features]
default = []
cuda = []
metal = []
vulkan = []
